<style type="text/css">
</style>

{# <pre> #}
{# {{ dump(data) }} #}
{# </pre> #}
<div class="contrat__content">

    <div class="flex space-between bold">
        <h2>ANNEXE 1 </h2>
        <p class="bold mb-20">N° de client : R{{ quoteRequest.postalCode.zone }} - {{ quoteRequest.customerId }}</p>
        {# <p>{% if quoteRequest.customerId is defined %}N°{{ quoteRequest.customerId }}{% endif %}</p> #}
    </div>
    <p class="mb-20">au contrat en date du {{ quoteRequest.dateUpdate|date('d/m/Y') }} conclu entre</p>
    <p><strong>{{ quoteRequest.businessName }}</strong></p>
    <p>et</p>
    <p class="bold mb-30">REISSWOLF Aktenvernichtungs-AG</p>

    <h3 style="margin-top: 60px;">INTERLOCUTEURS</h3>

    {# INTERLOCUTEUR 1 #}
    <p class="bold mt-20">Interlocuteurs en région :</p>
    <div class="flex">
        <div style="margin-right: 30px;"><p>Mme. Emmanuelle Lugand (Resp. Administrative)</p>
            <p style="text-align: left;">M. Pascal Laperrousaz (Directeur Général)</p>
        </div>
        <div>
            <p>Tél. : +41 22 552 62 62</p>
            <p>Tél. : +41 22 552 62 62</p>
            <p>E-Mail : geneve@reisswolf.ch</p>
        </div>
    </div>

    <p class="bold mt-20">Responsable de la protection des données :</p>
    <div class="flex">
        <div style="margin-right: 30px;">
            <p style="text-align: left;">Mme Saskia Strahberger (DPO)</p>
        </div>
        <div>
            <p>Tél. : +41 61 511 25 38</p>
            <p>E-Mail : dpo@reisswolf.ch</p>
        </div>
    </div>

    {# INTERLOCUTEUR 2 #}
    <p class="bold mt-20 mb-20">Interlocuteur {{ quoteRequest.businessName }}</p>
    {# ajouter dans le formulaire une possibilité de saisir le nom du signataire N°2)
    Et donc reprendre ici les deux signataires #}
    <div class="flex">
        <div style="margin-right: 30px;">
            <p style="text-align: left;">
                {% if quoteRequest.civility == 'M' %}
                    {{ 'General.M'|trans([], null, 'FR') }}
                {% elseif quoteRequest.civility == 'Mme' %}
                    {{ 'General.Mme'|trans([], null, 'FR') }}
                {% endif %}  {{ quoteRequest.firstName }} {{ quoteRequest.lastName }}
            </p>
        </div>
        <div>
            <p>Tél. : {{ quoteRequest.phone }}</p>
            <p>E-Mail : {{ quoteRequest.email }}</p>
        </div>
    </div>
</div>
